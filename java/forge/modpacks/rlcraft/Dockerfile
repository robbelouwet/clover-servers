# BUILD CONTEXT IS REPOSITORY ROOT

ARG ARCH=linux/arm64
ARG FORGE_VERSION
ARG MINECRAFT_VERSION

FROM --platform=${ARCH} robbelouwet/forge-dedicated:${MINECRAFT_VERSION}-${FORGE_VERSION}
ARG DOWNLOAD_LINK
ARG ARG_MOD_VERSION
ARG DEBIAN_FRONTEND=noninteractive
LABEL MOD_VERSION=${ARG_MOD_VERSION}

COPY archives/${ARCHIVE} /rlcraft.zip
RUN 
